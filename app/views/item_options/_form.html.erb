<%= form_for(@item_option) do |f| %>
  <% if @item_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_option.errors.count, "error") %> prohibited this item_option from being saved:</h2>

      <ul>
      <% @item_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
